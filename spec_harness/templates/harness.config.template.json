{
  "schema_version": 2,
  "_comment": "Template de perfil do SDD Spec Harness. Copiado para .claude/spec_harness/harness.config.json por `init-repo`. O motor é global (~/.claude/spec_harness) e não tem nada de stack hardcoded: é ESTE arquivo que adapta o harness ao repositório. Ajuste tudo marcado com TODO antes do primeiro autorun.",
  "scaffold": {
    "_comment": "Comando de teste que o scaffold-packet escreve no packet da spec. {test_paths} = os arquivos de teste da seção '## Arquivos permitidos'. TODO: ajuste ao runner deste repo (ex.: 'npm test -- {test_paths}', 'go test ./...', 'pytest -q {test_paths}'). O comando precisa falhar (exit != 0) quando o teste da fase RED falha, e não abortar na coleta. O binário do comando precisa estar rastreado pelo git (ou em worktree.copy_paths/link_paths) — o worktree da spec nasce da branch, não da árvore de trabalho, e o doctor reprova um binário só local para evitar a fase morrendo com exit 127 no meio do autorun.",
    "test_command_template": "pytest -q --no-cov -p no:cacheprovider {test_paths} -m \"not llm_integration\""
  },
  "docs": {
    "_comment": "Opcional. Injeta no prompt de cada fase a lista exata de docs de `docs/` (ou onde o repo guardar convenções) que ELA precisa — o resto de `docs/` fica fora do escopo da fase e a sessão não lê. Sem esta seção nada é injetado, sem custo nenhum. Vale a pena quando o repo tem doc grande (dezenas de KB) que a sessão releria em todo turno da fase via @import do CLAUDE.md: essa releitura, paga pelo cache a cada turno, costuma ser o maior item de custo do autorun num repo assim. TODO: preencha só se este repo tiver esse tipo de doc; caso contrário apague a seção inteira.",
    "por_fase": {
      "red": [],
      "green": [],
      "verify": []
    }
  },
  "scopes": {
    "_comment": "'app' do packet. Uma spec toca UM escopo. 'paths' = prefixos que o escopo pode ler/escrever; prefixos repetidos entre escopos são compartilhados e não geram erro de cruzamento. TODO: troque pelos escopos reais deste repo.",
    "TODO_dominio": {
      "paths": [
        "src/TODO_dominio/",
        "tests/"
      ]
    },
    "TODO_shared": {
      "paths": [
        "src/shared/",
        "tests/"
      ]
    }
  },
  "source_extensions": [
    ".py"
  ],
  "test_markers": {
    "_comment": "RED só pode escrever paths que casem com algum destes; GREEN não pode escrever nenhum deles.",
    "patterns": [
      "/tests/",
      "/test_",
      "test_",
      "conftest.py",
      "fixtures"
    ]
  },
  "validators": {
    "_comment": "Rodados no GREEN e no VERIFY sobre os arquivos alterados. {files} = lista de arquivos alterados (relativos à raiz), filtrada por source_extensions. Use null para desligar um validador que a base ainda não sustenta como gate.",
    "lint": {
      "id": "global_lint",
      "run": "ruff check {files}"
    },
    "format": null,
    "typecheck": null
  },
  "worktree": {
    "_comment": "copy_paths traz para cada worktree o que não é versionado e o worktree precisa. Para runtime Claude, inclua .claude/settings.json para o hook PreToolUse; em Codex não é necessário. TODO: acrescente .env e afins.",
    "link_paths": [],
    "copy_paths": []
  },
  "implementer": {
    "_comment": "Sessões headless que implementam RED e GREEN dentro do worktree da spec. runtime='codex' usa `codex exec`; runtime='claude' preserva o runner Claude Code e exige .claude/settings.json no worktree para o hook. VERIFY não abre sessão.",
    "runtime": "codex",
    "model": null,
    "sandbox": "workspace-write",
    "approve_for_me": true,
    "timeout_ms": 2400000,
    "permission_mode": "acceptEdits",
    "allowed_tools": "Read Grep Glob Bash Write Edit",
    "_allowed_tools_comment": "Sem TodoWrite de propósito: ninguém lê a lista de todos de uma sessão headless (o orquestrador só vê uma linha por tentativa e o resumo final), então cada chamada é turno pago sem efeito em gate nem em revisão.",
    "max_attempts": 2,
    "_max_attempts_comment": "Tentativas por fase antes de devolver o controle ao orquestrador. A tentativa seguinte recebe os erros do gate como feedback, no lugar de {feedback}.",
    "max_budget_usd": null,
    "_max_budget_usd_comment": "Opcional. Passa --max-budget-usd para a sessão headless de fase: teto de gasto em dólar POR INVOCAÇÃO (RED, ou GREEN/retomada, ou cada nova tentativa), imposto pela própria CLI. Freio de emergência contra uma sessão presa em loop dentro do timeout de 40min sem convergir — não é limite de qualidade, é limite de conta. Comece alto (ex.: 2) e desça só depois de ver o custo real de uma spec típica; um teto baixo demais mata a sessão no meio de um GREEN legítimo e o autorun conta isso como tentativa gasta.",
    "reuse_session": true,
    "_reuse_session_comment": "Uma única sessão headless por spec: o GREEN retoma a do RED e cada tentativa retoma a anterior, em vez de abrir sessão fria. O que a assinatura cobra é contexto NOVO (cache_creation + output); reler o prefixo é cache read, que não conta. Retomar troca o piso de contexto de cada fase nova (dezenas de milhares de tokens) + a releitura da spec e dos arquivos de orientação por quase zero. O enforcement não afrouxa: o hook decide pela run ATIVA, que o harness troca entre as fases, então o GREEN continua sem conseguir escrever em arquivo de teste mesmo compartilhando a sessão com o RED. Numa retomada o prompt usado é `prompts.retomada`, não o da fase. Sem esse prompt no perfil, o reaproveitamento se desliga sozinho (AVISO do doctor) — upgrade do motor não quebra repositório já configurado. Para desligar de propósito: false.",
    "lean_context": true,
    "_lean_context_comment": "Corta desperdício do contexto das sessões de fase, sem remover nenhuma instrução que o modelo use: `--strict-mcp-config` (MCP não serve a uma fase) e `--setting-sources project,local`, que descarta os plugins de nível user. O ganho principal aparece quando algum plugin de terminal tem um PostToolUse que falha em sessão headless (sem /dev/tty): o Claude Code grava stdout+stderr em contexto a cada tool call, um custo imprevisível por spec. O hook de path scoping do spec-harness é reinjetado por `--settings`, por caminho absoluto, porque sem ele o GREEN poderia reescrever o teste do RED. Para desligar: false.",
    "system_prompt": null,
    "_system_prompt_comment": "OPCIONAL e desligado por padrão. Substitui o system prompt inteiro do Claude Code, cortando ainda mais o piso de cada turno — mas remove também as diretrizes de uso de ferramenta e de execução cuidadosa, que é trocar qualidade por token. Só preencha se medir que a qualidade não caiu.",
    "prompts": {
      "red": "Você está implementando a fase {phase} da spec {spec} neste worktree ({worktree}), escopo {app}.\n\nLeia a spec inteira primeiro — ela é a fonte da verdade. Requisitos desta fase: {requirements}.\n\nVocê só pode LER:\n  - {read_paths}\n\nRegras invioláveis: as do CLAUDE.md/AGENTS.md na raiz deste repositório, incluindo a regra de dependências entre camadas.\n\nAmbiente: ative o ambiente do repositório antes de rodar os testes.\nComando de teste desta spec (use exatamente este): {test_command}\n\nNÃO commite, não crie branch, não rode git além de leitura. O harness commita a fase sozinho quando os gates passarem. Não escreva relatório nenhum: termine quando o comando de teste estiver no estado exigido abaixo.\n\nResultado da tentativa anterior desta fase:\n{feedback}\n\nFASE RED — escreva o teste que comprova a AUSÊNCIA do comportamento pedido pela spec.\n\nVocê só pode ESCREVER:\n  - {write_paths}\nNão toque em nenhum arquivo de produção: isso é a fase GREEN, e o gate reprova se você tocar.\n\nO teste tem de falhar pelo motivo certo. Em Python isso exige cuidado: um teste que importa um módulo inexistente no topo do arquivo quebra na COLETA (pytest sai 2), o que é indistinguível de um typo no import — e o gate recusa. Se o módulo/função ainda não existe, faça o import DENTRO do corpo do teste, para que a ausência vire uma falha de teste normal (exit 1). Se a spec altera comportamento de código que já existe, mantenha o import no topo e escreva o assert que falha.\n\nTermine quando `{test_command}` falhar exatamente pela ausência do comportamento — nunca por erro de sintaxe, fixture faltando ou import quebrado.",
      "green": "Você está implementando a fase {phase} da spec {spec} neste worktree ({worktree}), escopo {app}.\n\nLeia a spec inteira primeiro — ela é a fonte da verdade. Requisitos desta fase: {requirements}.\n\nVocê só pode LER:\n  - {read_paths}\n\nRegras invioláveis: as do CLAUDE.md/AGENTS.md na raiz deste repositório, incluindo a regra de dependências entre camadas.\n\nAmbiente: ative o ambiente do repositório antes de rodar os testes.\nComando de teste desta spec (use exatamente este): {test_command}\n\nNÃO commite, não crie branch, não rode git além de leitura. O harness commita a fase sozinho quando os gates passarem. Não escreva relatório nenhum: termine quando o comando de teste estiver no estado exigido abaixo.\n\nResultado da tentativa anterior desta fase:\n{feedback}\n\nFASE GREEN — implemente o MÍNIMO de código de produção que faz o teste da fase RED passar.\n\nVocê só pode ESCREVER:\n  - {write_paths}\nNão altere nenhum arquivo de teste: o teste da fase RED é o contrato, e o gate reprova se você mexer nele. Se o teste parecer errado, pare e explique — não o edite.\n\nNão implemente nada além do que os requisitos desta fase pedem: escopo extra é reprovado pelo gate de paths e desperdiça a revisão.\n\nTermine quando `{test_command}` passar e o lint do repositório estiver limpo nos arquivos que você tocou.",
      "_retomada_comment": "Usado em TODA invocação que retoma a sessão da spec (o GREEN depois do RED, e cada nova tentativa de uma fase). Curto de propósito: a sessão já tem a spec, o mapa do repositório e o teste do RED no contexto, e reenviar isso é justamente o contexto novo que consome cota. O bloco de write_paths NÃO é decorativo — é a fronteira que mudou ao trocar de fase.",
      "retomada": "Continuando na MESMA spec e no mesmo worktree ({worktree}). Você já leu a spec e já conhece os arquivos relevantes: não releia o que já está no seu contexto.\n\nAgora é a FASE {phase}. Requisitos desta fase: {requirements}.\n\nA partir de agora você só pode ESCREVER:\n  - {write_paths}\nIsso SUBSTITUI a permissão da fase anterior: os paths que você podia escrever antes agora estão bloqueados pelo hook. Se a fase anterior escreveu o teste, esse teste é o contrato e você não consegue mais editá-lo — não tente, e se ele parecer errado, pare e explique em vez de contorná-lo.\n\nComando de teste (use exatamente este): {test_command}\n\nResultado da vez anterior:\n{feedback}\n\nTermine quando `{test_command}` estiver no estado que esta fase exige e o lint estiver limpo nos arquivos que você tocou. Não commite, não crie branch, não escreva relatório — o harness commita sozinho quando os gates passarem."
    }
  }
}
